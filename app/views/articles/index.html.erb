<h1 align="center"> ALL ARTICLES</h1>

<p align="center"> <%= link_to "CREATE NEW ARTICLE", new_article_path, class:"btn btn-success" %></p> 
  
<% @articles.each do |article| %>
<div class="row">
<div class="container">
<div class="card text-center">


<div class="card-header bg-danger">
<%= article.title %>
</div>
<div class="card-body">
<h5 class="card-title">Discription:</h5>
<p class="card-text">
<%= article.discription %></p>
</div>

<div class="card-footer text-muted bg-primary">
<%= link_to 'Edit', edit_article_path(article),class: "btn btn-success" %>

<%= link_to 'Show', article_path(article), class: "btn btn-dark" %>
<%= link_to 'Delete', article_path(article), method: :delete, data:{confirm:"are you sure"}, class:"btn btn-danger"%>
</div>
</div>
<% end %>
</div>
</div>





